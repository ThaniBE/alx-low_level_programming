#include "main.h"

/**
 * mul - multiplies two integers
 *
 * @a: first integer
 * @b: second integer
 *
 * Return: product of a and b
 */
int mul(int a, int b)
{
	for (a = 48; a < 58; a++)
	{
		for (b = 48; b < 58; b++)
			;
	}
	return (a * b);
}

